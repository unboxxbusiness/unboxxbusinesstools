import type { Metadata } from "next";

const title = "Landing Page Headline Generator";
const description = "Generate effective, attention-grabbing one-line headlines for your coaching landing pages based on your course and key highlights.";

export const metadata: Metadata = {
  title,
  description,
  openGraph: {
    title: `${title} | UBX Tools`,
    description,
  },
  twitter: {
    title: `${title} | UBX Tools`,
    description,
  },
  alternates: {
    canonical: "/tools/landing-page-headline-generator",
  },
};

export default function ToolLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
