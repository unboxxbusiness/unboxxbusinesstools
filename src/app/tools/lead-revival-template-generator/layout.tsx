import type { Metadata } from "next";

const title = "Lead Revival Template Generator";
const description = "Generate non-pushy message templates for cold leads who stopped responding, helping you to re-engage old inquiries effectively.";

export const metadata: Metadata = {
  title,
  description,
  openGraph: {
    title: `${title} | UBX Tools`,
    description,
  },
  twitter: {
    title: `${title} | UBX Tools`,
    description,
  },
  alternates: {
    canonical: "/tools/lead-revival-template-generator",
  },
};

export default function ToolLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
