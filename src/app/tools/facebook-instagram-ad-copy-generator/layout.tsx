import type { Metadata } from "next";

const title = "Facebook/Instagram Ad Copy Generator";
const description = "Generate short, medium, and long ad copy templates based on your course and offer, saving you time on ad creation.";

export const metadata: Metadata = {
  title,
  description,
  openGraph: {
    title: `${title} | Unboxx Business Tools`,
    description,
  },
  twitter: {
    title: `${title} | Unboxx Business Tools`,
    description,
  },
  alternates: {
    canonical: "/tools/facebook-instagram-ad-copy-generator",
  },
};

export default function ToolLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
